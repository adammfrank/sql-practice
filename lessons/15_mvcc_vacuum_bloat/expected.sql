-- Not used for row-correctness in this lesson (the gate compares dead-tuple
-- counts before/after your solution, not query results). Kept for
-- convention/consistency with the other lessons' probe query.
SELECT 1;
