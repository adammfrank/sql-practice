-- TODO: Write your query here. Replace the placeholder below.
-- Goal: select (category, revenue) from the materialized view you create
-- in indexes.sql (see README.md) -- not from the live tables.
SELECT 1;
