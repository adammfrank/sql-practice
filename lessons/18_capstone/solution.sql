-- TODO: write the fast version of the report described in README.md.
-- It must return exactly the same 10 rows, in the same order, as
-- expected.sql, but hit the performance target by using the indexes you
-- create in indexes.sql (and, if you like, by rewriting the query).
--
-- This stub returns the wrong rows on purpose, so the lesson fails until
-- you solve it.
SELECT 1;
